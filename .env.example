# =============================================================================
# CONFIGURAÇÕES DO SISTEMA RPA - BASE AUTOMATIONS
# =============================================================================
#
# Este arquivo contém as variáveis de ambiente necessárias para executar
# o sistema de automação. Copie este arquivo para .env e preencha com
# suas credenciais reais.
#
# IMPORTANTE: Nunca commite o arquivo .env com credenciais reais!
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO AZURE KEY VAULT (RECOMENDADO)
# -----------------------------------------------------------------------------
# Habilita o uso do Azure Key Vault para gerenciamento seguro de credenciais
# Se habilitado, as credenciais serão buscadas do Key Vault primeiro,
# com fallback para variáveis de ambiente caso não encontradas
USE_AZURE_KEYVAULT=false

# URL do Azure Key Vault (ex: https://seu-keyvault.vault.azure.net/)
AZURE_KEYVAULT_URL=

# Credenciais do Azure (Service Principal - opcional se usar Managed Identity)
# Estas variáveis são necessárias apenas se não estiver usando Managed Identity
# ou Azure CLI authentication
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=

# NOMES DOS SEGREDOS NO KEY VAULT:
# Ao usar Key Vault, os segredos devem ter os seguintes nomes (use hyphens):
# - EMAIL-SENDER
# - EMAIL-PASSWORD
# - EMAIL-FAILURE-RECIPIENT
# - EMAIL-SMTP-SERVER
# - TWOCAPTCHA-API-KEY
# - DATABRICKS-HOST
# - DATABRICKS-HTTP-PATH
# - DATABRICKS-ACCESS-TOKEN
# - WEBHOOK-EXECUTION-STATUS

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE EMAIL (Fallback se não usar Key Vault)
# -----------------------------------------------------------------------------
# NOTA: É altamente recomendado usar Azure Key Vault para armazenar estas credenciais

# Servidor SMTP para envio de emails
EMAIL_SMTP_SERVER=smtp.hostinger.com

# Email remetente (usado para enviar notificações)
EMAIL_SENDER=seu_email@exemplo.com

# Senha do email remetente
EMAIL_PASSWORD=sua_senha_aqui

# Email destinatário para notificações de falha
EMAIL_FAILURE_RECIPIENT=destinatario@exemplo.com

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO 2CAPTCHA (Fallback se não usar Key Vault)
# -----------------------------------------------------------------------------
# NOTA: É altamente recomendado usar Azure Key Vault para armazenar esta credencial

# API Key do serviço 2Captcha para resolução de captchas
# Obtenha sua chave em: https://2captcha.com/
TWOCAPTCHA_API_KEY=sua_chave_2captcha_aqui

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO SISTEMA
# -----------------------------------------------------------------------------
# Nome do robô/processo RPA
ROBOT_NAME=ProcessadorRPA

# Ambiente de execução (development, staging, production)
ENVIRONMENT=development

# Versão do projeto/bot
PROJECT_VERSION=1.0.0

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE DIRETÓRIOS
# -----------------------------------------------------------------------------
# Diretório base para downloads (usar caminhos relativos para compatibilidade)
DOWNLOAD_PATH=downloads

# Diretório para logs
LOG_PATH=logs

# Diretório para backups
BACKUP_PATH=backups

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO DATABRICKS (Fallback se não usar Key Vault)
# -----------------------------------------------------------------------------
# NOTA: É altamente recomendado usar Azure Key Vault para armazenar estas credenciais

DATABRICKS_HOST=
DATABRICKS_HTTP_PATH=
DATABRICKS_ACCESS_TOKEN=

# -----------------------------------------------------------------------------
# WEBHOOKS E TELEMETRIA (Fallback se não usar Key Vault)
# -----------------------------------------------------------------------------
WEBHOOK_EXECUTION_STATUS=

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS
# -----------------------------------------------------------------------------
# Timeout padrão para operações (em segundos)
DEFAULT_TIMEOUT=30

# Número máximo de tentativas para operações
MAX_RETRIES=3

# Tempo de espera entre tentativas (em segundos)
RETRY_WAIT_TIME=2

# Modo headless para navegador (True/False)
BROWSER_HEADLESS=False
